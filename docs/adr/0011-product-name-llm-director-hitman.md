# 对外产品名

对外统一称 **LLM Director Hitman**（demo、README、录屏、pitch 页、HUD 标题）。

不再使用 archive pitch 草案中的 **Opportunity Engine** 作为对外名；若需副标题可用：

```text
LLM Director Hitman — LLM-directed assassination sandbox demo
```

Repo 目录名 `llm_director_hitman` 与对外名一致。
